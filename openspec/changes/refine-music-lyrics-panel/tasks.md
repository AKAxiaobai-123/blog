## 1. 歌词区布局重构

- [x] 1.1 调整 `.music-progress` 为独立列布局：歌词行 `flex: 1`、进度行 range 填满剩余宽度、时间右对齐
- [x] 1.2 统一歌词行/进度条/时间的宽度与对齐，进度条延伸至时间左侧，消除右侧无效留白
- [x] 1.3 歌词行单行省略号截断，无歌词显示“暂无歌词”、音频错误显示“音频待补充”

## 2. 展开缩放按钮

- [x] 2.1 新增展开按钮（图标 + `aria-expanded` + `aria-controls`），无脚本时保持禁用
- [x] 2.2 点击切换根容器 `is-expanded` class，脚本同步 `aria-expanded` 状态
- [x] 2.3 实现歌词行与进度行的平滑缩放过渡，`prefers-reduced-motion` 下直接切换

## 3. 验证与交付

- [x] 3.1 验证桌面、移动端、暗色、无脚本与减少动效场景，音乐条整体宽度保持不变
- [ ] 3.2 提交并推送到 `dev` 分支
